Thư mục này dùng để chứa các file voice-over: slide-01.mp3 … slide-12.mp3

Đây là PLACEHOLDER — chưa có file âm thanh thật trong gói này (không thể tạo file
âm thanh trong lần build này). audio-manager.js đã được viết để bỏ qua an toàn
khi thiếu file, nên bài học vẫn chạy đầy đủ mà không có tiếng.

Khi có file thu âm thật, chỉ cần đặt đúng tên file vào thư mục này, không cần sửa code.
